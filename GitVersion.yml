mode: Mainline
branches:
  master:
    increment: Patch
    tracks-release-branches: true
  release:
    regex: ^release?[/-]
    source-branches: ['main', 'master']
    is-release-branch: true
ignore:
  sha: []
merge-message-formats: {}